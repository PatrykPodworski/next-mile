query getProduct($slug: String!) {
  product(where: { slug: $slug }) {
    ...ProductDetails
  }
}

fragment ProductDetails on Product {
  slug
  name
  price
  description
  images(first: 1) {
    url
  }
}
